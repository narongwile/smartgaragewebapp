version: '3'

services:
  app:
    # Build dockerfile
    build: .
    # Restart container in case of crashes etc
    restart: always
    # Set API to use host networking
    network_mode: host
    # API depends on DB to be there
    # depends_on:
    #   - db
    # Set env variables
    environment:
      APP_KEY: jlG45rVkU1PSx7DBngfjQ3RfozrY_kk0
      SESSION_DRIVER: cookie
      DB_CONNECTION: mssql
      MSSQL_SERVER: smartgaragedbserver.database.windows.net
      MSSQL_PORT: 1433
      MSSQL_USER: nrkwine
      MSSQL_PASSWORD: NRK@wine2544
      MSSQL_DB_NAME: smartgaragedb

      # PG_PASSWORD: example
      # PG_USER: postgres

#   db:
#     # Set DB version to run
#     image: postgres:13.3-alpine
#     # Restart container in case of crashes etc
#     restart: always
#     # Set DB to use host networking
#     network_mode: host
#     # Set DB env variables
#     environment:
#       POSTGRES_PASSWORD: example
#     # Mount DB data to volume, 
#     # so we don't lose all DB data over deployments
#     volumes:
#       - database:/var/lib/postgresql/data

# # Define the DB volume
# volumes:
#   database: